# yaml-language-server: $schema=https://raw.githubusercontent.com/alik-avizha/content-schema/refs/heads/main/schemas/nutrack.schema.json
id: "cmia8ejsy000700i06cde1fdssd"
title: "Single-spa: Microfrontend Architecture"
description: |
  ## single-spa — архитектура микрофронтендов

  **single-spa** — это JavaScript-фреймворк (оркестратор), предназначенный для построения
  **микрофронтенд-архитектуры**, при которой несколько независимых SPA
  могут работать **одновременно на одной странице**.

  Он не заменяет React, Vue или Angular, а управляет тем,
  **когда и какое приложение загружать, монтировать и размонтировать**.

  ---

  ## Зачем нужен single-spa

  single-spa решает ключевые проблемы больших фронтенд-приложений:

  - 🧩 разделение монолита на независимые модули
  - 🚀 независимый деплой частей приложения
  - 👥 работа нескольких команд параллельно
  - 🔄 постепенная миграция между фреймворками
  - 📦 уменьшение связности кода

  ---

  ## Основные понятия

  ### Root Config
  Центральное приложение-оркестратор, которое:
  - регистрирует микрофронтенды
  - управляет маршрутизацией
  - контролирует порядок загрузки

  Root config **не содержит бизнес-логики**.

  ---

  ### Microfrontend (Application)
  Отдельное SPA, которое:
  - разрабатывается независимо
  - деплоится отдельно
  - знает только про себя
  - экспортирует lifecycle-функции

  Возможные технологии:
  - React
  - Vue
  - Angular
  - Vanilla JS

  ---

  ## Жизненный цикл приложения

  Каждое приложение в single-spa должно экспортировать:

  - `bootstrap` — вызывается один раз при первой загрузке
  - `mount` — вызывается при активации приложения
  - `unmount` — вызывается при деактивации

  single-spa сам управляет вызовом этих этапов.

  > Это позволяет безопасно подключать и отключать микрофронтенды
  > без перезагрузки страницы.

  ---

  ## Маршрутизация

  single-spa использует правило **activeWhen** для определения,
  когда приложение должно быть активным.

  Приложение может:
  - быть привязано к пути (`/profile`)
  - быть всегда активным (navbar, header)
  - активироваться функцией

  Каждый микрофронтенд **сам выбирает роутер**
  (React Router, Vue Router и т.д.).

  ---

  ## Layout Engine

  Layout Engine позволяет декларативно описывать структуру страницы:

  - фиксировать navbar
  - управлять DOM-иерархией
  - избегать конфликтов разметки

  Это рекомендуемый способ организации layout в production.

  ---

  ## Загрузка приложений

  single-spa обычно используется с:
  - **SystemJS**
  - **Import Maps**

  Это позволяет:
  - контролировать версии
  - переиспользовать зависимости
  - деплоить микрофронтенды независимо

  ---

  ## Работа с зависимостями

  Рекомендуется:
  - делить React / ReactDOM
  - делить UI-kit
  - НЕ делить бизнес-логику

  Общие зависимости подключаются на уровне root config.

  ---

  ## Взаимодействие между микрофронтендами

  Основные подходы:
  - custom events
  - shared state (store)
  - API-ориентированное взаимодействие

  ❌ Не рекомендуется:
  - прямой импорт одного микрофронтенда в другой

  ---

  ## Когда single-spa подходит

  - большое приложение
  - несколько команд
  - долгий цикл жизни проекта
  - необходимость независимых деплоев

  ## Когда не подходит

  - маленький проект
  - одна команда
  - простой SPA без роста

  ---
  
  ## Основные понятия

  ### Root Config
  Центральное приложение-оркестратор, которое:
  - регистрирует микрофронтенды
  - управляет маршрутизацией
  - контролирует порядок загрузки

  Root config **не содержит бизнес-логики**.

  #### Пример root-config

  ```ts
  import { registerApplication, start } from 'single-spa';

  registerApplication({
    name: '@app/navbar',
    app: () => System.import('@app/navbar'),
    activeWhen: ['/'],
  });

  registerApplication({
    name: '@app/content',
    app: () => System.import('@app/content'),
    activeWhen: location => location.pathname.startsWith('/app'),
  });

  start();
  
  ```ts

  ## Итог

  single-spa — это мощное архитектурное решение,
  которое требует дисциплины, но дает:
  - масштабируемость
  - независимость
  - контроль над ростом приложения

activities:
  - "activity-2"
